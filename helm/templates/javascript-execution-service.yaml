apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-javascript-execution-service
  labels:
    app: {{ .Release.Name }}-javascript-execution-service
spec:
  replicas: {{ .Values.javascript.replicaCount }}
  selector:
    matchLabels:
      app: {{ .Release.Name }}-javascript-execution-service
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}-javascript-execution-service
    spec:
      containers:
        # Main Javascript Execution Service Container
        - name: main
          image: "{{ .Values.javascript.image.repository }}:{{ .Values.javascript.image.tag }}"
          imagePullPolicy: {{ .Values.javascript.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.javascript.service.port }}
              protocol: TCP
          env:
            - name: DOCKER_HOST
              value: tcp://localhost:2375
          volumeMounts:
            - name: shared-workspace
              mountPath: /workspace
          resources:
            {{- toYaml .Values.javascript.resources | nindent 12 }}

        # Docker in Docker (DinD) Sidecar Container
        - name: dind-sidecar
          image: "{{ .Values.javascript.sidecar.image.repository }}:{{ .Values.javascript.sidecar.image.tag }}"
          imagePullPolicy: {{ .Values.javascript.sidecar.image.pullPolicy }}
          securityContext:
            privileged: true
          env:
            - name: DOCKER_TLS_CERTDIR
              value: ""
          volumeMounts:
            - name: shared-workspace
              mountPath: /workspace
          readinessProbe:
            exec:
              command:
                - sh
                - -c
                - docker image inspect {{ .Values.javascript.sidecar.runnerImage }} >/dev/null 2>&1
            initialDelaySeconds: 5
            periodSeconds: 5
          lifecycle:
            postStart:
              exec:
                command:
                  - sh
                  - -c
                  - |
                    until docker info >/dev/null 2>&1; do
                      sleep 1
                    done
                    docker pull {{ .Values.javascript.sidecar.runnerImage }}
      volumes:
        - name: shared-workspace
          emptyDir: {}
---
apiVersion: v1
kind: Service
metadata:
  name: {{ .Release.Name }}-javascript-execution-service
  labels:
    app: {{ .Release.Name }}-javascript-execution-service
spec:
  type: {{ .Values.javascript.service.type }}
  ports:
    - port: {{ .Values.javascript.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ .Release.Name }}-javascript-execution-service
